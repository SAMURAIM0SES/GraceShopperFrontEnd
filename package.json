{
  "name": "react-starting-setup",
  "version": "0.1.0",
 
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.3.0",
    "@testing-library/user-event": "^13.5.0",
    "nanoid": "^4.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^4.4.0",
    "react-paginate": "^8.1.3",
    "react-router-dom": "^6.3.0",
    "react-scripts": "5.0.1",
    "react-stripe-checkout": "^2.6.3",
    "stripe": "^10.2.0",
    "stripe-checkout": "^1.0.9",
    "web-vitals": "^2.1.4",
    "@faker-js/faker": "^7.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "dev": "NODE_ENV=development npm run compile-env && npm run start",
    "compile-env": "node compile-env.js",
    "build": "NODE_ENV=production npm run compile-env && react-scripts build"
  },

  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
  
  
}
